#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_yarn@1.21.1@yarn/bin/yarn.js" "$@"
  ret=$?
else 
  node  "$basedir/../_yarn@1.21.1@yarn/bin/yarn.js" "$@"
  ret=$?
fi
exit $ret
